# RoboDiffusion

RoboDiffusion is a sibling package to Sonata that implements a Diffusion Policy-style
controller for RoboPianist using RP1M action supervision.

All code and outputs for RoboDiffusion live under this package so the active
`Sonata/` and `robopianist/` trees remain untouched.
